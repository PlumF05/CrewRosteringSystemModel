# 变更记录（Changelog）

> 本文件记录本项目的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
> 版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
>
> 当前为**首个版本 v0.1**（预发布），因此本文件仅记录该版本；后续版本将在此基础上追加。

## [0.1.0] - 2026-09-17

**首个版本**：核心链路闭环——课程表导入 → 自动排班 → 排班表导出。Windows 单机桌面应用，数据保存在本机，无后端服务器。

### 新增

**助理管理**
- 助理的增删改查、学号唯一约束、身份类型（本科生 / 研究生）、联系方式与班级
- **个性化值班节数**：可为单个或多个助理单独设置值班节数上下限，覆盖全局默认；未单独设置的助理沿用全局
- 删除助理时级联清理其课程表与排班记录，不产生孤儿数据

**课程表导入**
- 解析学院标准课程表 xlsx（含合并单元格、多行课程单元格、多段周次、单周/双周写法）
- **全自动归档**：学号已存在则更新姓名与身份类型（保留电话、QQ、班级），不存在则直接建档；两条分支收口为「整体替换该助理课程表」，结果一致且可重复导入
- 导入后**自动检测与既有排班的冲突**，列出冲突明细并提供「删除冲突排班」「去排班表重新生成」两个动作
- 无法解析的条目进入警告清单而非静默丢弃

**排班规则配置**
- 排班周期、每人工时上下限、同时段人数上下限、工作日、上班节次区间
- 课程类别过滤（理论课 / 实验课是否计入占用）、排班模式（uniform）、最少连续值班节数
- **每日均衡**规则（默认开启）：在不违反其他约束的前提下消除「某天无人值班」
- **规则一致性校验**：周期颠倒、上下限倒置、节次区间重叠等非法配置被拦截并给出明确错误

**自动排班**
- 贪心启发式：时段按候选人数先难后易、候选按「延续连续块 → 负载均衡 → 稀缺度 → id」排序
- 「工时修复 ⇄ 孤立块撤销」迭代收敛，解决撤销后不再补位导致的缺节问题
- 每日均衡两档策略：零损伤补齐优先，容量耗尽时启用让位式搬移（源日不得变为空白天）
- 输出**确定性可重放**：同输入必同输出

**排班表与导出**
- 逐周预览、写入全部周、每人工时汇总（含各自目标区间）、未满足时段清单（附原因与上课时间）
- 重排前后的差异对比
- 导出 xlsx（兼容 Excel / WPS），带版式（标题、底色、网格线、自动换行）
- 值班格子「姓名 + 电话」上下两行显示；节次行标注上课时间；提供「含电话」开关
- 节次体系与课程表编号一致（上午 1~4、下午 6~9），命名节次（中课/晚课）参与精确占用判定

**桌面端与界面**
- Tauri v2 打包：Windows 可执行文件与 NSIS 安装包
- 导入/导出走**系统文件对话框**（浏览器环境自动回退为文件选择/下载）
- 使用帮助面板（快速上手、各模块操作说明、常见问题）、关于系统（版本与作者）、操作日志与仪表盘
- 帮助面板内嵌**从教务系统导出课程表的图文步骤**（三步操作截图，点击可放大并连续翻页）

**工程与测试**
- 160 项自动化测试：算法、数据层、解析器、导出、冲突检测、数据库迁移、真实课表夹具集成测试
- 脱敏课程表样例（`samples/`）作为解析器集成测试夹具，兼顾回归覆盖与个人信息保护
- 排错手册（13 条真实缺陷与陷阱记录）、需求规格说明书、架构决策记录

### 修复

- **在学生上课时间排班**：课程表与排班表的节次编号相差 2（课程表「第6节」= 排班表「第8节」），占用判定按节号比较时漏判；现统一按节次序号比较，并建立序号轴单一数据源
- **数据库升级导致整库不可用**：升级中平移带唯一索引的节次键会撞击复合唯一索引并使事务回滚；改为「归档到快照 + 记录日志 + 清空」，由重新生成还原
- **删除助理遗留孤儿排班**：界面与导出中出现 `#id` 而非姓名
- **已写入排班在界面与导出中「隐身」**：修改上班节次后，网格行列仅按当前规则推导导致数据不可见；改为按「当前规则 ∪ 已写入数据」取并集
- **课程表脱敏漏改**：单元格文本被拆分为多个文本片段时，按片段替换会漏掉学号且整串检测无法发现；改为按单元格整体替换后按原片段长度回填
- 周次解析不支持单周/双周写法导致整条课程被丢弃
- 规则缺少一致性校验时静默产出空结果（界面表现为「点了没反应」）
- 网格列顺序依赖对象整型键序的脆弱实现

### 变更

- **导出个人信息口径收紧**：仅携带电话号码一项，学号与 QQ 一律不写入导出文件（有全量断言保护）
- Excel 依赖由社区版 `xlsx` 切换为 `xlsx-js-style`：社区版无法写入单元格样式，无法实现「姓名/电话上下排列」与版式美化
- 课程表导入取消人工确认步骤，改为全自动归档
- **仓库内敏感信息脱敏**：回归夹具（`regression-3assistants.json`）、单元测试数据与文档示例中的真实姓名与学号一律替换为占位符（`示例学生A/B/C`、`2026000001~3`）；`samples/` 下的课程表样例已做三层脱敏（表头姓名学号、`上课教师` 字段、文档属性元数据），可随仓库公开分发
- 项目**版本号由 1.0.0 调整为 0.1.0**：首个版本按预发布编号，避免对功能完整度产生误解

### 已知限制

- **手动调整 + 冲突校验 + 撤销/重做（F04）尚未实现**：`pinia` 与快照仓储已就位，界面未开发
- 导出模板管理（F07）尚未实现，表头与格式目前为内置固定样式
- 数据备份 / 恢复、多文件批量导入、多周合并导出尚未实现
- 桌面端目前仅产出 NSIS 安装包，未产出 MSI
- 界面层（`.vue` 组件）无自动化测试覆盖，依赖人工走查
