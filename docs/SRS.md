# 学院行政办助理排班系统

## 软件需求规格说明书

---

## 1 引言

### 1.1 编写目的

本文档为"学院行政办助理排班系统"的软件需求规格说明书，旨在明确系统的功能需求、非功能需求、数据需求和接口需求，为后续的系统设计、编码实现和测试验收提供依据。本文档的读者包括项目负责人、软件开发人员、测试人员及课程指导教师。

### 1.2 项目背景

学院行政办公室每学期需安排学生助理协助处理日常行政事务。助理来源为学生群体，涵盖本科生和研究生，各自课程安排不同，空闲时间各异。传统的人工排班方式效率低、易出错，且难以兼顾各类约束条件（如每人最短/最长上班时间、同时段排班人数限制等）。为此，设计本排班系统，通过信息化手段实现排班自动化，提高排班效率与准确性。

### 1.3 术语定义

| 术语 | 定义 |
|------|------|
| 助理 | 指被安排在行政办协助处理事务的学生，包括本科生和研究生 |
| 排班 | 指将助理分配到具体时间段的工作安排过程 |
| 课程表 | 指学生个人的课程安排，包含课程名称、时间、周次、教室等信息 |
| 排班表 | 指最终的助理工作安排结果，展示每位助理在各时间段的值班情况 |
| 约束条件 | 指排班过程中必须满足的限制规则，如最短/最长工作时间、同时段人数限制等 |
| 自动排班 | 指系统根据预设规则和助理课程表自动计算最优排班方案的功能 |

### 1.4 参考资料

[1] GB/T 9385-2008 计算机软件需求规格说明规范

[2] 软件工程：实践者的研究方法（第9版），Roger S. Pressman著

[3] 学院行政办助理排班系统项目计划书

[4] 标准课程表样例文件（附件，Word 格式，仅作数据结构参考）

[5] 架构决策记录 `docs/ADR-001.md`、`docs/ADR-002.md`

---

## 2 项目概述

### 2.1 项目目标

本项目的目标是开发一个**单机桌面排班系统**，实现学院行政办助理排班的自动化和规范化。具体目标包括：

1. 支持学生课程表的导入与解析，自动识别助理的空闲时间段；
2. 根据预设排班规则和约束条件，自动完成助理排班；
3. 提供手动调整功能，允许管理员对自动排班结果进行微调；
4. 支持排班表的导出（如Excel格式），便于打印和分发；
5. 提供助理个人信息管理和课程表管理功能。

### 2.2 系统架构概述

本系统为**单机桌面应用**（Windows 平台，经 Tauri v2 打包），不依赖后端服务器，所有数据保存在本机。系统整体架构分为以下层次：

1. **表现层**：基于 Vue 3 + Element Plus 的图形界面，以 Tauri WebView 窗口呈现，提供课程表导入、排班查看、手动调整等操作入口；
2. **业务逻辑层**：实现排班算法（贪心启发式）、约束校验、数据管理等核心业务逻辑；
3. **数据层**：使用浏览器本地数据库 IndexedDB（经 Dexie.js 封装），存储助理信息、课程表数据、排班规则配置、排班结果及操作日志等数据。

> 技术选型的完整决策过程见 `docs/ADR-001.md`（存储与打包）与 `docs/ADR-002.md`（框架、算法与撤销机制）。

### 2.3 用户特征

本系统的主要用户为学院行政办公室的工作人员（排班管理员）。用户应具备基本的计算机操作能力，无需专业的编程知识。系统界面应简洁直观，操作流程清晰。

### 2.4 假设与依赖

1. 假设学院已提供标准化的学生课程表文件格式；系统导入以本项目提供的**标准 Excel 模板**为准，Word 格式的原始课程表需先转换为 .xlsx（附转换指引）；
2. 假设助理人数在合理范围内（不超过50人），系统性能可满足实时排班需求；
3. 依赖运行环境：Windows 10 及以上系统（自带 WebView2 渲染引擎）。

---

## 3 功能需求

### 3.1 自动排班功能（F01）

系统应能根据以下输入自动完成排班：

- **输入**：所有助理的课程表数据、排班规则配置、排班时间段设置；
- **处理**：系统识别每位助理的课程空闲时间，结合排班约束条件（每人最短/最长工作时间、同时段人数限制等），采用**贪心启发式排班算法**自动分配助理到各时段（算法选型与流程见 ADR-002 第三节）；
- **输出**：生成初始排班表，展示每位助理在各时间段的值班安排。

**优先级：高。** 这是系统的核心功能，应优先实现。

### 3.2 助理信息管理功能（F02）

系统应提供助理个人信息的录入、查看、修改和删除功能。助理信息字段包括：

1. **姓名**：助理的真实姓名；
2. **学号**：学院统一学号；
3. **身份类型**：本科生/研究生（不同身份可能对应不同的排班规则）；
4. **联系电话**：手机号码；
5. **QQ号**：用于即时通讯联系；
6. **班级**：所属班级信息；
7. **课程表文件**：上传该助理的个人课程表文件。

> 2026-09-11 增补：课程表导入时若解析出的学生（姓名+学号）在系统中不存在，系统应支持**按学生信息一键创建助理**，身份由课程条目标记（（本）/（研））自动推导，创建后自动选为导入归属。
>
> 2026-09-13 修订：导入课程表时的助理归档改为**全自动**，取消人工确认步骤。系统按表头"学生：姓名(学号)"自动归属：① **学号已存在** → 直接用解析结果更新该助理的姓名与身份类型（课程表无法提供的电话/QQ/班级一律保留不动），随后导入课程表；② **学号不存在** → 直接新建助理，随后导入课程表。两条分支最终收口到同一动作（整体替换该助理的课程表），因此**结果一致、重复导入幂等**，且不会产生重复档案。若文件未包含学生姓名或学号，则无法自动归属，系统提示原因且本次不写入任何数据。

### 3.3 排班规则配置功能（F03）

系统应允许管理员配置以下排班规则：

1. **每人最短上班时间**：设置每位助理在一个排班周期内最少需要值班的小时数/天数；
2. **每人最长上班时间**：设置每位助理在一个排班周期内最多可以值班的小时数/天数；
3. **同时段排班人数要求**：设置同一时间段内最少和最多需要多少名助理值班；
4. **排班原则**：可配置排班优先级策略（如按身份类型优先、按空闲时间优先、轮询分配等）；
5. **排班周期**：设置排班覆盖的周次范围（如第1周至第17周）；
6. **上班时间表**（2026-09-11 增补）：设置每周工作日（默认周一至周五）与上班节次（默认上午第 1~4 节、下午第 6~9 节）；排班只发生在工作日的上班节次内，**排班时段按单节划分**（第 1 节、第 2 节……各自为独立排班时段）并按节次显示；
7. **排班模式**（2026-09-11 增补）：可选择"各周独立排班"（按每周课程占用分别计算）或"各周排班相同"（以整个周期内任一周有课即视为占用，生成一份排班后复制到全部周次——导出时仅需单周表）；
8. **课程占用过滤**（2026-09-11 增补）：可选择理论课（样例标记（本）/（研））与实验课（标记（实））是否计入值班占用；
9. **最少连续排班节次**（2026-09-11 增补）：可设置每次值班的最少连续节数（默认 1 = 不限制）。大于 1 时，孤立的单节值班不被安排——例如助理第 9~10 节有课、最少连续 2 节时，第 11 节（其可值班连续段仅剩 1 节）不会排给他；自动排班输出中不足最少连续节数的孤立安排会被自动撤销（手动指定的除外）。
10. **每日均衡排班**（2026-09-13 增补）：可开启"每日均衡"（默认开启）。开启后，系统应在**不违反课程占用、人数上限、最少连续节数、每人工时上限**的前提下尽量平均每日排班，避免出现某天无人值班。例如：周一至周四的排班已符合要求但周五无人值班时，系统应调整排班使周五也有人值班。调整分两档执行：
    - **A 档（零损伤，优先）**：① 为助理在该天新建一个满足最少连续节数的连续块；② 把某助理在别天的整个连续块搬到该天空闲连续段（该助理工时净变化为 0）。两种操作都**只允许动用冗余值班**（源时段移出后仍满足同时段人数下限），不新增任何"未满足时段"。
    - **B 档（兜底让位，仅在 A 档无解时启用）**：当某工作日**一个人都没有**、而 A 档手段全部失效时（典型场景是工时上限已用尽：如 3 人 × 上限 6 节 = 18 人节，却需覆盖 5 天 × 8 节 = 40 个时段），允许搬走整个连续块，把源时段的覆盖让出来以保证"每天都有人"；被让出的时段进入**未满足时段清单**交由手动调整兜底。硬性闸门：**源日让出后必须仍有至少 1 条排班**，绝不把"空白天"转移到另一日期。
    - **取舍说明**：当总工时容量小于"覆盖全部时段"所需人节数时，"每个时段都有人"在数学上不可达，此时"每天都有人"与"每时段都有人"不可兼得。系统显式选择前者（即本需求的核心目标），并保证不新增空白天、不突破任何硬约束、排班总量与每人工时守恒。若 A 档可行则一律走 A 档，配置宽裕时行为与 B 档无关。
    - 调整后若仍不存在可行的调整方案，则保持原结果不变（"尽量"语义）。

> 2026-09-13 增补（节次时间标注）：系统内置各节次的上课时间——第1节 8:00~8:45、第2节 8:50~9:35、第3节 9:55~10:40、第4节 10:45~11:30、第6节 14:00~14:45、第7节 14:50~15:35、第8节 15:40~16:25、第9节 16:45~17:30。排班表页面的每一行节次、以及导出文件的每一行节次，都在节号后附带该时间（如"上午 第1节 8:00~8:45"）；未登记时间的节次（管理员自行添加的区间）只显示节号，不留空占位。该时间表为系统内置常量（非可配置项），单一数据源位于 `src/algorithms/types.ts`（`SECTION_TIME`），避免多处硬编码漂移。
>
> 2026-09-13 增补（按助理个性化值班节数）：支持为单个或多个助理单独设置值班节数上下限，覆盖全局默认；未单独设置的助理沿用全局默认。设置入口在「助理管理」的助理编辑表单（"个性化值班节数"区域，开关开启后自动以全局默认值预填）；设置结果随助理档案持久化，刷新后仍生效，并在排班生成时立即生效——"每人工时"汇总中会显示每位助理各自的目标区间（个性化者带标识）。校验：最少 ≤ 最多、最少非负、上限至少为 1；关闭"单独设置"将清除覆盖值、恢复跟随全局（不会残留旧值继续生效）。
>
> 2026-09-13 增补（F06 第 3 条落地，冲突检测）：导入课程表成功后，系统应检测该生**已写入的排班**是否落在其有课的节次上（判定口径与排班算法一致：节次序号区间 + 理论课/实验课过滤）。检出冲突时应：列出冲突明细（周次/星期/节次/课程）并提供「删除这些冲突排班」与「去排班表重新生成」两个动作；删除与跳转均记录操作日志。未检出冲突时不提示。
>
> 2026-09-13 增补（数据完整性）：删除助理时应**级联删除**其课程表与排班记录（此前只清理课程表，留下引用已删除助理的"孤儿排班"，会在界面与导出文件中显示为"#id"）。
>
> 2026-09-13 修正（节次编号对齐，错误修复）：经实测核对，学院课程表的节次是**"数字节次 + 命名节次"混排**的连续序列——上午第 1~5 节、午间"中课1/中课2"、下午第 6~10 节、傍晚"晚课"、晚上第 11~13 节（顺序取自课程表 A 列时段块）。原设计的上班节次把下午编为第 8~11 节，导致**同一节真实课程在两表中编号不同**（课程表"第6节"是下午第一节，排班表却称"第8节"），占用判定按节号比较时会漏判，出现"在学生上课时间排班"。现修正为：**排班表的上班节次编号与课程表完全一致（上午第 1~4 节、下午第 6~9 节）**；课程占用判定统一换算到"当日先后序号"后比较，命名节次（中课1=6、中课2=7、晚课=13）同样参与占用计算，不再一律按"整天占用"处理。节次序号轴与时间表的单一数据源分别为 `src/utils/sectionOrder.ts`（`SECTION_SEQUENCE`）与 `src/algorithms/types.ts`（`SECTION_TIME`）。存量配置中的旧默认"下午 8~11 节"会在读取规则时自动对齐为"下午 6~9 节"，存量排班记录随之平移，已有的自定义节次不受影响。

### 3.1 补充（2026-09-11）：一次性完成全周期排班

F01 应支持**按排班周期一次性生成全部周次的排班方案**：逐周根据该周课程占用计算可用性；"各周排班相同"模式下生成一份方案并复制到周期内所有周次。写入时按周落库。

### 3.4 手动调整功能（F04）

系统应提供手动调整排班结果的能力：

1. 管理员可手动将某位助理分配到指定时间段；
2. 管理员可移除某位助理在指定时间段的值班安排；
3. 手动调整后系统应自动校验是否违反已配置的排班约束条件，并在违反时给出警告提示；
4. 支持排班记录的撤销/重做操作（采用**快照栈**机制实现，见 ADR-002 第四节）。

### 3.5 数据导入导出功能（F05）

系统应提供数据的导入和导出能力：

1. **课程表导入**：支持批量导入学生课程表文件（按标准 Excel 模板填写），系统自动解析并录入；Word 格式（.doc/.docx）不直接支持，需先转换为 .xlsx；
2. **排班表导出**：支持将最终排班结果导出为Excel文件，便于打印和线下分发；
3. 导出内容应包括：日期、时间段、值班助理姓名、联系方式等关键信息。

> 2026-09-11 增补：导出时**可选择是否包含助理个人信息**（学号/电话/QQ）。包含时导出"排班表"与"值班明细"两个工作表；不包含时仅导出"排班表"网格（值班人姓名可见，联系方式不写入文件），用于对个人信息敏感的场合。
>
> 2026-09-13 修订：个人信息口径收紧为**只携带"电话号码"这一项**——学号与 QQ 一律不写入导出文件。联系电话在导出时**默认自动填入**排班表的每个值班格子（文案格式「姓名（电话）」），并配套生成"值班明细"工作表（周次/星期/节次/姓名/电话）。导出前可关闭"含电话"开关，此时仅输出姓名、不生成明细表。同时取消原先的导出选项对话框，改为即点即导，减少操作步骤。
>
> 2026-09-13 修订（版式与可读性）：导出排班表的每一个节次行均标注上课时间（见 F03 第 2026-09-13 增补）；值班格子中助理的**姓名与电话改为上下两行**显示（单元格开启自动换行），同一时段多人时同样按行堆叠；标题、表头、网格施加加粗/居中/细边框/底色等版式，便于打印与分发。因需写入单元格样式，Excel 读写库由 SheetJS 社区版切换为其直系分支 `xlsx-js-style`（读写 API 兼容，详见 ADR-001 第五节）。

### 3.6 课程表管理功能（F06）

系统应提供课程表的查看和管理能力：

1. 允许管理员手动修改某位助理的课程表（如课程时间临时调整）；
2. 支持查看任意助理的课程表详情；
3. 课程表修改后，系统应提示是否需要同步更新排班结果。

### 3.7 排班表模板管理功能（F07）

系统应提供排班表和课程表模板的配置管理：

1. 可设置和修改空白排班表的格式模板（如表头字段、时间段划分、列宽等）；
2. 可设置和修改课程表导入模板的格式，以适配不同学期或不同格式的课程表文件；
3. 模板修改后，新导入的数据将自动按新模板格式展示。

### 3.8 重新排班功能（F08）

当新增助理或排班规则发生变更时，系统应支持重新执行排班算法：

1. 新增助理后，可选择对现有排班表进行增量调整或全量重新排班；
2. 重新排班时保留已手动调整且未冲突的安排（通过排班记录的来源标记区分），仅对未分配或冲突的时段重新计算；
3. 提供排班结果对比功能，基于重排前后的排班快照差异实现（见 ADR-002 第四节）。

---

## 4 非功能需求

### 4.1 性能需求

1. 系统应在5秒内完成一次完整排班计算（助理人数<=50人时）；
2. 课程表导入解析应在3秒内完成（单文件）；
3. 页面响应时间不超过2秒。

### 4.2 安全性需求

1. 系统为单机本地应用，不设登录认证，通过操作系统账户权限与本机物理接触控制访问；
2. 助理的联系电话、QQ号等个人信息仅存储于本机；导出的排班表包含联系方式时，系统应提示用户妥善保管文件；
3. 排班数据的修改应记录操作日志（本地存储），便于追溯。

### 4.3 可用性需求

1. 系统界面应简洁友好，操作步骤不超过3步即可完成常见操作；
2. 系统应提供操作提示信息，帮助用户理解当前操作状态；
3. 应用以独立桌面窗口运行（Tauri + WebView2），要求 Windows 10 及以上系统。

### 4.4 可维护性需求

1. 源代码应编写清晰的注释，遵循统一的编码规范；
2. 系统架构应模块化，各功能模块之间耦合度低，便于独立修改和扩展；
3. 应提供配置文件管理排班规则，避免硬编码。

---

## 5 数据需求

### 5.1 课程表数据结构

课程表数据是系统排班的基础输入。根据标准课程表样例，每门课程包含以下信息：

1. **课程编号**：如"10125121047"，用于唯一标识课程；
2. **课程名称**：如"软件工程[02]"；
3. **上课周次**：如"1-4周"、"6-17周"，表示课程生效的周次范围；
4. **上课节次**：如"第一节-第二节"、"第三节-第五节"等；
5. **上课星期**：星期一至星期日；
6. **上课教师**：授课教师姓名；
7. **上课地点**：如"南院-博学东楼-303"。

### 5.2 排班表数据结构

排班表是系统的核心输出，包含以下信息：

1. **日期/周次**：排班覆盖的具体周次；
2. **时间段**：如"第一节-第二节"、"第三节-第五节"等；
3. **值班助理**：被安排到该时段的助理姓名及学号；
4. **值班地点**：行政办公室位置；
5. **备注**：特殊情况说明。

### 5.3 助理信息数据结构

助理信息表包含以下字段：

| 字段名 | 类型 | 约束 |
|--------|------|------|
| 助理ID | 字符串 | 系统内部唯一标识 |
| 姓名 | 字符串 | 非空 |
| 学号 | 字符串 | 唯一 |
| 身份类型 | 枚举值 | 本科生/研究生 |
| 手机号 | 字符串 | 格式校验 |
| QQ号 | 字符串 | - |
| 班级 | 字符串 | - |
| 关联课程表文件路径 | 字符串 | - |
| 个性化值班节数 | 数值（可选，成对：最少/最多） | 缺省跟随全局；最少 ≤ 最多 |

---

## 6 接口需求

### 6.1 用户界面

系统以桌面窗口（Tauri WebView）作为用户界面，主要页面包括：

1. **仪表盘**：展示排班概览、待办事项等；
2. **助理管理页面**：助理信息的增删改查；
3. **课程表管理页面**：课程表导入、查看、修改；
4. **排班配置页面**：排班规则设置；
5. **排班表页面**：自动排班执行、手动调整、排班结果查看与导出。

### 6.2 文件接口

1. **课程表导入接口**：接受按标准格式填写的 Excel 文件（.xlsx，由学院原始 Word 课程表转换而来，格式定义见附录A），系统自动解析；.doc/.docx 等 Word 格式不直接解析，需先转换为 .xlsx（附转换指引）；
2. **排班表导出接口**：输出标准 Excel 格式文件（.xlsx），包含格式化的排班表格。

### 6.3 数据库接口

系统使用浏览器本地数据库 IndexedDB（经 Dexie.js 封装）存储结构化数据，包括助理信息表、课程表数据表、排班规则配置表、排班结果表、操作日志表、排班快照表等，不使用服务器端数据库。选型依据见 ADR-001。

---

## 7 附录

### 附录A：标准课程表样例说明

学院原始课程表样例为 Word 格式（`timeTableForStu12.doc`），转换后的 `timeTableForStu12.xlsx`（位于项目根目录）作为系统导入的实际输入格式。其结构要点如下（已实际核验）：

1. **表头区**：第 1 行为标题"我的课程表"；第 2 行含学期信息（如"2026-2027-1"）与学生姓名和学号（如"学生：庞士豪(1024005228)"）；
2. **特殊信息区**："上课时间暂未确定的课程"与"调课信息"以自由文本形式出现在网格上方，解析时应跳过并标记提示；
3. **网格区**：列为"节次/星期"与星期一至星期日（共 9 列），行为节次段（如"第一节-第二节"、"第三节-第五节"）；
4. **课程单元格**：为多行文本，格式为"（身份）课程编号-课程名称[班次]"换行后接周次信息（如"1-4周"、"10周"）；
5. **合并单元格**：跨节次/跨节的课程以合并单元格表达（样例含 65 个合并区域），解析时需按合并范围展开。

**解析实现要求**：处理合并单元格展开、单元格内多行文本拆分、周次范围解析（如"1-4周"、"10周"、"5-17周单周"等变体），并对无法解析的单元格给出行级错误提示。
